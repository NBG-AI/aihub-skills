# Refined Request: User-Level NBG Design Skill

## Category
Pi Agent skill packaging, configuration portability, and reusable presentation design-system enablement.

## Objective
Create a reusable user-level Pi skill named **"NBG Design"** from the existing project-level NBG presentation configuration and design-system content so it can be invoked from any working directory, not only from `/Users/giorgosmarinos/contentwork/nbg-theme-agent`.

The downstream work should package the project’s NBG presentation context, rules, templates, and required assets into the appropriate user-level Pi skill location while preserving the project’s current NBG presentation-generation behavior and avoiding machine/project-root coupling in the skill’s runtime instructions.

## Scope

### In Scope
- Identify the current project-level artifacts that define the NBG presentation design context, including at minimum:
  - `config/pi-agent-nbg-design.yaml`
  - `NBG-Design/NBG Design System.html`
  - `NBG-Design/slide-templates.jsx`
  - `NBG-Design/tweaks-panel.jsx`
  - required files under `NBG-Design/assets/`
  - relevant screenshots under `NBG-Design/screenshots/` that are needed as visual references
  - relevant project documentation that explains expected usage and guardrails, especially `docs/design/project-design.md`, `docs/design/project-functions.MD`, and `docs/design/configuration-guide.md`
- Determine the correct user-level Pi skill destination for the local environment. Based on current local evidence, the likely user-level skill root is:
  - `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/`
- Create or update a Pi skill whose human-facing name is **"NBG Design"** and whose filesystem/metadata slug is likely `nbg-design`.
- Copy all files, data, and content required for the skill to work independently from the project folder into the user-level skill folder or its subfolders.
- Include a `SKILL.md` file in the user-level skill folder with valid Pi skill frontmatter and clear instructions for using the NBG design system.
- Ensure the skill instructions preserve the existing project behavior and guardrails, including:
  - use the NBG presentation design system and assets rather than inventing a parallel design system;
  - inspect the bundled design-system documentation/templates before generating final slide output;
  - default unspecified deck language to English (`en`), as already documented in the project exception log;
  - default unspecified final output format to HTML (`html`), as already documented in the project exception log;
  - ask for missing required presentation inputs instead of inventing fallback values;
  - keep newsletter/email prototypes out of scope unless explicitly requested;
  - for HTML slide output, fit the complete 16:9 / 1920×1080 composition within common browser viewports;
  - for HTML-to-PowerPoint conversion, recreate slides with native editable PowerPoint objects rather than embedding full-slide screenshots;
  - preserve continuous text blocks in PowerPoint recreations as continuous editable text boxes.
- Make all skill-internal references resolve from the installed user-level skill folder, not from the original project root.
- Avoid copying secrets, credentials, tokens, machine-local browser paths, caches, generated bytecode, virtual environments, version-control metadata, or unrelated generated deliverables unless explicitly required.
- Document any unresolved assumptions, omissions, or follow-up decisions in the appropriate project documentation or pending-items file if implementation proceeds downstream.

### Out of Scope
- Implementing the skill during this refinement step.
- Changing the original NBG visual design system or source assets except as needed for packaging/copying into the user-level skill.
- Reworking generated presentations under `presentations/` unless they are intentionally selected as examples or references for the skill package.
- Copying the entire project repository into the user-level skill when a smaller, purposeful skill package is sufficient.
- Storing API keys, provider credentials, Pi login data, expiring tokens, or other secrets in the skill folder.
- Creating fallback configuration values for required settings beyond already documented user-approved defaults.
- Performing version-control operations.
- Creating or modifying a project-level skill development folder unless downstream work changes the skill itself and project conventions require dedicated skill-development documentation.

## Requirements

1. **Source Artifact Inventory**
   - Review the project-level configuration and design-system files to identify the minimum required content for a standalone user-level NBG Design skill.
   - At minimum, evaluate whether the following must be included in the skill package:
     - `config/pi-agent-nbg-design.yaml`
     - `NBG-Design/NBG Design System.html`
     - `NBG-Design/slide-templates.jsx`
     - `NBG-Design/tweaks-panel.jsx`
     - `NBG-Design/assets/logo-primary.png`
     - `NBG-Design/assets/logo-knockout.png`
     - `NBG-Design/assets/logo-small.png`
     - NBG design-system photos under `NBG-Design/assets/`
     - representative screenshots under `NBG-Design/screenshots/`
   - Explicitly exclude non-required content such as `.git/`, `.venv/`, `__pycache__/`, project session state, and unrelated generated outputs.

2. **User-Level Skill Location**
   - Use the Pi user-level skills directory for this machine.
   - The likely target directory is `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/`, based on the existing user-level skill example at `/Users/giorgosmarinos/.pi/agent/skills/deep-dive-creator/SKILL.md`.
   - If Pi’s current configured skill directory differs, downstream work must detect and use the actual configured user-level skills directory.

3. **Skill Metadata and Name**
   - Create a valid Pi skill file at the target location, likely `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/SKILL.md`.
   - The skill must be discoverable as **NBG Design** to users.
   - The frontmatter `name` should use a Pi-compatible slug, likely `nbg-design`, unless Pi requires another naming format.
   - The frontmatter `description` must clearly state when to use the skill, e.g. for creating NBG-styled presentations, HTML slides, and editable PowerPoint recreations using the bundled NBG design system.

4. **Self-Contained Skill Content**
   - Copy required design-system files, assets, templates, and configuration content into the skill folder in a clear internal structure, for example:
     - `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/SKILL.md`
     - `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/config/pi-agent-nbg-design.yaml`
     - `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/NBG-Design/...`
   - The exact internal layout may differ if Pi skill conventions recommend another structure, but it must be documented and easy to inspect.
   - The installed skill must not depend on the original project directory remaining at `/Users/giorgosmarinos/contentwork/nbg-theme-agent`.

5. **Path Portability Inside the Skill**
   - Rewrite or adapt any copied configuration/instructions so file references are skill-relative or otherwise resolvable from the skill package.
   - Do not leave project-root-relative instructions that only work when the current working directory is the original repository root, unless the instruction explicitly explains that it refers to bundled paths inside the skill.
   - Do not introduce hardcoded machine-specific absolute paths for bundled skill resources.

6. **Preserve Existing NBG Presentation Rules**
   - Preserve the project’s NBG presentation defaults, style principles, color tokens, supported language guidance, template catalog, and output guardrails from `config/pi-agent-nbg-design.yaml`.
   - Preserve HTML viewport-fit guidance and HTML-to-PowerPoint native recreation guidance in the skill instructions.
   - Preserve the rule that missing required presentation inputs should be requested from the user rather than silently invented, except for the documented defaults of English language and HTML output format.

7. **Validation**
   - After downstream implementation, verify that the skill folder exists at the user-level target location and contains `SKILL.md` plus the required copied resources.
   - Verify that no required references inside `SKILL.md` or copied config point back to the original project root unless intentionally documented as provenance only.
   - Verify that the skill can be discovered or invoked by Pi according to the local Pi skill mechanism.
   - Verify that all required bundled asset paths referenced by the skill exist.
   - Verify that no secrets, credentials, `.venv`, `.git`, cache, or generated bytecode files were copied into the skill package.

8. **Documentation and Traceability**
   - If implementation proceeds, document the created user-level skill location, included source artifacts, excluded artifacts, and validation results.
   - If any ambiguity remains, record it as an open question or pending item rather than hiding it.

## Constraints
- This refined request is a specification only; do not implement the skill during the refinement step.
- Follow project conventions for downstream work:
  - reference material under `docs/reference/`,
  - plans under `docs/design/plan-xxx-<description>.md`,
  - complete design updates in `docs/design/project-design.md`,
  - feature/function updates in `docs/design/project-functions.MD`,
  - pending issues in `Issues - Pending Items.md`.
- If downstream work creates or changes a skill in the context of this project, project instructions require a dedicated folder named according to `[skill-name]-docs` for skill-development documentation.
- Do not perform version-control operations unless explicitly requested.
- Do not store secrets, credentials, API keys, Pi login tokens, or expiring tokens in the skill package.
- Do not create fallback configuration values for missing required settings unless the user explicitly approves and the exception is documented first.
- Any code scripts created downstream must follow project conventions; test scripts belong under `test_scripts/`.
- The user-level skill should be reusable from anywhere, so it must not require the shell current working directory to be the project root.

## Acceptance Criteria
- A refined specification exists at `docs/reference/refined-request-user-level-nbg-design-skill.md`.
- Downstream implementation creates or updates a user-level Pi skill named **NBG Design** with a Pi-compatible slug, likely `nbg-design`.
- The user-level skill is installed in the correct Pi user-level skills directory, likely `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/`.
- The installed skill contains a valid `SKILL.md` with appropriate frontmatter and clear usage instructions.
- All required NBG design-system configuration, templates, assets, data, and content needed for the skill are copied into the user-level skill package.
- The skill can be used from arbitrary working directories without requiring the original project path `/Users/giorgosmarinos/contentwork/nbg-theme-agent`.
- Skill references to bundled resources resolve correctly from the skill folder.
- Existing NBG presentation-generation guardrails are preserved, especially HTML viewport fitting and native editable HTML-to-PowerPoint recreation.
- The package excludes secrets, credentials, `.git/`, `.venv/`, caches, generated bytecode, and unrelated generated outputs.
- Validation evidence confirms the skill folder contents, path portability, required asset existence, absence of disallowed files, and Pi discoverability/invocation behavior.
- Any unresolved location, naming, or content-selection questions are documented before or during downstream implementation.

## Assumptions
- The active project root is `/Users/giorgosmarinos/contentwork/nbg-theme-agent`.
- The relevant project-level configuration source is `config/pi-agent-nbg-design.yaml`.
- The relevant project-level design-system source folder is `NBG-Design/`.
- The intended user-level Pi skills directory on this machine is `/Users/giorgosmarinos/.pi/agent/skills/`, based on the existing installed skill `deep-dive-creator`.
- The skill filesystem slug should be `nbg-design`, while the user-facing display name should be **NBG Design**.
- The phrase “user level skill” means a Pi skill installed under the user’s home directory and available across projects/shell working directories.
- The skill should be self-contained by copying required resources, not by symlinking to the active project folder, unless the user explicitly approves symlinks.
- The skill should package reusable NBG design-system guidance and assets, not generated presentation deliverables unless selected as examples.
- The already documented project defaults are still valid: unspecified deck language defaults to English and unspecified final output format defaults to HTML.

## Open Questions
1. Is `/Users/giorgosmarinos/.pi/agent/skills/nbg-design/` the confirmed target directory for user-level Pi skills, or does the current Pi installation use another skill root?
2. Should the skill include all files under `NBG-Design/`, or only the minimal subset required for presentation generation (`NBG Design System.html`, templates, assets, and selected screenshots)?
3. Should generated presentation examples under `presentations/` be copied into the skill as examples, or should they remain project-only artifacts?
4. Should documentation files such as `docs/design/configuration-guide.md`, `project-design.md`, and `project-functions.MD` be copied into the skill package, summarized inside `SKILL.md`, or referenced only as project provenance?
5. Should the skill package use physical copies only, or are symlinks acceptable for easier updates from the project source? The current assumption is physical copies for portability.
6. How should future updates to the project-level NBG design system be propagated to the user-level skill: manual recopy, a documented sync script, or a dedicated update command?
7. Should the skill include newsletter/email prototype files from `NBG-Design/nbg-gpt`, `NBG-Design/newsletter`, and `NBG-Design/notebooklm`, or exclude them by default consistent with current project configuration?

## Original Request Verbatim
> I want you to use the config from this porject to create a user level skill 
> I want you to name it "NBG Design"
> I want you to copy all the files, data and content required from the project level/folder to the user level/folders
> to make the skill easily accessible from everywhere

## Additional Context Provided With Request
- Active project root: `/Users/giorgosmarinos/contentwork/nbg-theme-agent`
- The intended outcome appears to be a reusable user-level Pi skill named `NBG Design` derived from this project's project-level configuration/content.
- Please identify assumptions, acceptance criteria, likely source and target locations, and open questions if any.
